{
  "name": "js-browserstack",
  "version": "1.0.0",
  "description": "Sample code repository to run selenium tests on browserstack using NodeJs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "single": "node ./tests/single.js --timeout=120000",
    "parallel": "node ./tests/parallel.js --timeout=120000",
    "local": "node ./tests/local.js --timeout=120000"
  },
  "dependencies": {
    "browserstack-local": "^1.4.9",
    "selenium-webdriver": "^4.1.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
